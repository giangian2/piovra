# The service modules are still empty skeletons: without this, a rule that finds no classes to
# check fails. Set it back to true (or delete this file) as soon as catalog, inventory, order and
# channel-config have code, otherwise a rule that silently stops applying goes unnoticed.
archRule.failOnEmptyShould=false
